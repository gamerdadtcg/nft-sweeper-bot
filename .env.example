# Live stream bot (existing)
OPENSEA_API_KEY=
DISCORD_WEBHOOK_URL=
# Optional bot fallback (higher RAM)
DISCORD_TOKEN=
DISCORD_CHANNEL_ID=

# Morning digest uses the same OpenSea + Discord vars.
# Optional override for digest-only webhook:
# DISCORD_DIGEST_WEBHOOK_URL=

# Safety — search/post only. Do not enable unless you add signed-buy code.
LIVE_BUY=false
